// Exportamos las rutas para tomar imagenes:

export let Path = {
    url:'http://localhost:4200/assets/'
}

// Exportaremos el enpont de la APIREST de firebase

export let Api = {
   url: 'https://marketplace-8a41f-default-rtdb.firebaseio.com/'
}